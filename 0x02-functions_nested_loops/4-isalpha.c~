#include <stdio.h>
/**
 *  _islower - checks for lowercase character.
 *
 * @c: The variable for the argument
 *
 * Return: 1 if true, 0 if false
 */

int _isalpha(int c)
{
if ((c >= 'a' && c <= 'z') || (c >= 'A' && c <= 'Z'))
{
return (1);
}
else
{
return (0);
}
}
